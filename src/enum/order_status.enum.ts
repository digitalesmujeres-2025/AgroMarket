export enum OrderStatus {
  CREATED = 'created',
  PENDING = 'pending',
  PROCESSING = 'processing',
  READY = 'ready for delivery',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  DISABLED = 'Disabled',
}
